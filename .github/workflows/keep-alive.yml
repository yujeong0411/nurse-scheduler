name: Keep Render Alive

on:
  schedule:
    - cron: '*/5 * * * *'    # 5분마다 실행

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -f ${{ secrets.BACKEND_URL }}/settings || true
