services:
  - type: web
    name: nurse-scheduler-api
    runtime: python
    rootDir: .
    buildCommand: uv sync --project backend --frozen && uv cache prune --ci
    startCommand: uv run --no-sync --project backend uvicorn backend.main:app --host 0.0.0.0 --port $PORT
